import CPU from "./CPU";
import GPU from "./GPU";

const Proccessors = () => {
    return (
        <div>
            <CPU />
            <GPU />
        </div>
    );
};

export default Proccessors;
